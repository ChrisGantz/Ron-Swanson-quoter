import React from 'react';

const QuoteSize = () => {
  return (
    <div>
      <button>Large</button>
      <button>Medium</button>
      <button>Small</button>
    </div>
  );
};

export default QuoteSize;